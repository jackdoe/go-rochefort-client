language: go

go:
  - 1.7.x
  - 1.8.x
  - 1.9.x
  - master

services:
  - docker

env:
  - ROCHEFORT_TEST=http://localhost:8000

before_install:
  - docker pull jackdoe/rochefort
  - docker run -d -p 127.0.0.1:8000:8000 jackdoe/rochefort

script: go test ./...
