language: go

go:
  - 1.8.x
  - 1.9.x
  - master

services:
  - docker

env:
  - ROCHEFORT_TEST=http://localhost:8000

before_install:
  - docker pull jackdoe/rochefort:1.0
  - docker run -d -p 127.0.0.1:8000:8000 jackdoe/rochefort:1.0

script: go test ./...
